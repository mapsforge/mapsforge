configurations { providedCompile }

dependencies {
    compile project(":mapsforge-poi-reader")
    compile group: 'org.xerial', name: 'sqlite-jdbc', version: '3.8.11.2'
    providedCompile group: 'org.openstreetmap.osmosis', name: 'osmosis-core', version: '0.44.1'
}

sourceSets.main.compileClasspath += configurations.providedCompile

jar {
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    from('src/main/config/') {
        include 'default.properties'
        include 'plugin.xml'
        include 'poi-mapping.xml'
        include 'osmosis-plugins.conf'
        filter {
            line -> line.replace('${poi.writer.version}', version)
        }
    }
}
