# See: /path/to/a

# Create speed optimized ('release') debug optimized ('debug') binaries 
APP_OPTIM := release

# Create libraries for a specific architecture (armeabi armeabi-v7a x86).
# Here we use 'all' to create libraries for all available architectures.
# You can change this value to match only one architecture to save memory.
APP_ABI := all 
